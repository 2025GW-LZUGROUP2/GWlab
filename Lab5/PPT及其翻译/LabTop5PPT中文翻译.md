【批次1处理结果：第1-20页图片内容】
### 第1页内容
- 英文文本："Lab Topic 5"
  [实验主题5]


### 第2页内容
- 英文文本："Learning objectives"
  [学习目标]
- 英文文本："Use a PSO code provided to you"
  [使用提供给你的粒子群优化（PSO）代码]
- 英文文本："Apply PSO to implement GLRT for the quadratic chirp in colored Gaussian noise problem (review Lab 4)"
  [应用粒子群优化对 colored Gaussian 噪声问题中的二次 chirp 实现广义似然比检验（复习实验4）]
- 英文文本："Supplementary:"
  [补充内容：]
- 英文文本："Learn to use structures in Matlab"
  [学习在Matlab中使用结构体]
- 英文文本："Learn to use function handles in Matlab"
  [学习在Matlab中使用函数句柄]


### 第3页内容
- 英文文本："PSO codes"
  [粒子群优化代码]
- 英文文本："Clone the repository: GitHub →SDMBIGDAT19 (Store it outside GWSC)"
  [克隆代码库：GitHub →SDMBIGDAT19（将其存储在GWSC外）]
- 英文文本："Lectures delivered at the BigDat19 5th International Winter school on Big Data, Cambridge University, UK (Jan, 2019)"
  [讲座内容来自2019年1月于英国剑桥大学举办的第5届大数据国际冬令营（BigDat19）]
- 英文文本："The codes and slides supplement the textbook; Cover PSO and stochastic optimization in greater depth (including fundamental theorems)"
  [代码和幻灯片是对教材的补充；更深入地涵盖粒子群优化和随机优化（包括基本定理）]


### 第4页内容
- 英文文本："We will need the following codes in SDMBIGDAT19/CODES:"
  [我们将需要SDMBIGDAT19/CODES中的以下代码：]
- 英文文本："r2ss.m: Helper function; no need to look inside"
  [r2ss.m：辅助函数；无需查看内部代码]
- 英文文本："r2sv.m: Helper function; no need to look inside"
  [r2sv.m：辅助函数；无需查看内部代码]
- 英文文本："s2rs.m: Helper function; no need to look inside"
  [s2rs.m：辅助函数；无需查看内部代码]
- 英文文本："s2rv.m: Helper function; no need to look inside"
  [s2rv.m：辅助函数；无需查看内部代码]
- 英文文本："crcbchkstdsrchrng.m: Helper function; no need to look inside"
  [crcbchkstdsrchrng.m：辅助函数；无需查看内部代码]
- 英文文本："crcbpso.m: Main PSO code that can be applied to any fitness function"
  [crcbpso.m：可应用于任意适应度函数的粒子群优化主代码]
- 英文文本："crcbpsotestfunc.m: A benchmark fitness function; Also an example for how to code fitness functions to work with crcbpso.m"
  [crcbpsotestfunc.m：基准适应度函数；同时也是示例，展示如何编写能与 crcbpso.m 配合使用的适应度函数]
- 英文文本："crcbqcfitfunc.m: The fitness function for quadratic chirp GLRT (in WGN)"
  [crcbqcfitfunc.m：用于白噪声（WGN）中二次 chirp 广义似然比检验的适应度函数]
- 英文文本："crcbqcpsc.m: Applies PSO to the quadratic chirp fitness function"
  [crcbqcpsc.m：将粒子群优化应用于二次 chirp 适应度函数]
- 英文文本："test_crcbpso.m: Test function for crcbpso.m"
  [test_crcbpso.m：crcbpso.m 的测试函数]
- 英文文本："test_crcbqcpsc.m: Test function for crcbqcpsc.m"
  [test_crcbqcpsc.m：crcbqcpsc.m 的测试函数]


### 第5页内容
- 英文文本："Exercise #1 Part 1"
  [练习1 第一部分]
- 英文文本："Read the short user manual CODES/CodeDoc.pdf"
  [阅读简版用户手册 CODES/CodeDoc.pdf]
- 英文文本："The main usage instructions are in the \""help\"" for each function"
  [每个函数的主要使用说明在其“帮助”文档中]
- 英文文本："The test_<funcName>.m scripts show examples of usage for some of the functions"
  [test_<函数名>.m 脚本展示了部分函数的使用示例]
- 英文文本："The test_crcbpso.m script shows how crcbpso.m is applied to a benchmark fitness function (defined in crcbpsotestfunc.m)"
  [test_crcbpso.m 脚本展示了如何将 crcbpso.m 应用于基准适应度函数（在 crcbpsotestfunc.m 中定义）]


### 第6页内容
- 英文文本："Exercise #1 Part 2"
  [练习1 第二部分]
- 英文文本："To use the PSO code, the following are required"
  [要使用粒子群优化代码，需满足以下要求]
- 英文文本："Understand the concept of structures in Matlab"
  [理解Matlab中结构体的概念]
- 英文文本："Matlab structures work in the same way as structures in C"
  [Matlab 结构体的工作方式与C语言中的结构体相同]
- 英文文本："X = struct(\'a\', 5.0, \'b\', 6.0);"
  [X = struct('a', 5.0, 'b', 6.0);]
- 英文文本："disp(X.a) will show 5.0"
  [disp(X.a) 将显示 5.0]
- 英文文本："disp(X.b) will show 6.0"
  [disp(X.b) 将显示 6.0]
- 英文文本："Structures offer a convenient way to move a large number of arguments into and out of a function"
  [结构体为向函数传入或传出大量参数提供了便捷方式]
- 英文文本："Structures also help make your codes future - proof: New versions of codes can use new input arguments while old versions will ignore them"
  [结构体也有助于让代码具有“前瞻性”：新版本代码可使用新输入参数，而旧版本会忽略它们]


### 第7页内容
- 英文文本："Tasks: Set 1"
  [任务：第一组]
- 英文文本："Objective: Modify the function you wrote for generating your assigned signal to use a \'struct\' as the input argument for specifying the signal parameters"
  [目标：修改你为生成指定信号编写的函数，使用“结构体”作为指定信号参数的输入参数]
- 英文文本："For example, if your function was called foo(dataX, SNR, list_of_parameters), where \'list_of_parameters\' was either a vector (as in SIGNALS/crcbgenqcsig.m) or a set of input arguments, then pass the parameters inside a struct (P):"
  [例如，如果你的函数名为 foo(dataX, SNR, list_of_parameters)，其中 'list_of_parameters' 是向量（如 SIGNALS/crcbgenqcsig.m 中所示）或一组输入参数，那么将参数放入结构体 (P) 中传递：]
- 英文文本："Modify foo(dataX, SNR, list_of_parameters) to foo_new(dataX, SNR, P), where P is a struct"
  [将 foo(dataX, SNR, list_of_parameters) 修改为 foo_new(dataX, SNR, P)，其中 P 是结构体]
- 英文文本："For example, if your parameters were \(a_1, a_2, a_3\), the struct would be:"
  [例如，如果你的参数是 \(a_1, a_2, a_3\)，结构体应为：]
- 英文文本："P = struct(\'meaningful_name_of_param1\', \(a_1\), \'meaningful_name_of_param2\', \(a_2\), ...) where you should have field names that tell the reader of the code something useful about the nature of the parameter"
  [P = struct('meaningful_name_of_param1', \(a_1\), 'meaningful_name_of_param2', \(a_2\), ...) 其中，你应为字段命名，让代码阅读者能从中了解参数的性质信息]
- 英文文本："You can look at SDMBIGDAT19/CODES/s2rv.m to see how a struct is passed to a function and how its is used inside it"
  [你可以查看 SDMBIGDAT19/CODES/s2rv.m，了解结构体如何传递给函数以及在函数内部如何使用]
- 英文文本："Compare the outputs of foo_new to foo for the same input values and make sure they are identical"
  [对相同输入值，比较 foo_new 和 foo 的输出，确保两者一致]
- 英文文本："Create a test script to run foo_new that makes plots of (a) the signal time series with time axis in seconds, and (b) the signal periodogram for positive frequencies with the frequency axis in Hz"
  [创建测试脚本以运行 foo_new，绘制 (a) 时间轴为秒的信号时间序列，以及 (b) 频率轴为Hz的正频率信号周期图]


### 第8页内容
- 英文文本："Exercise #1 Part 3"
  [练习1 第三部分]
- 英文文本："Understand the concept of function handle"
  [理解函数句柄的概念]
- 英文文本："Encountered earlier: quadratic noise PSD"
  [之前遇到过：二次噪声功率谱密度（PSD）]
- 英文文本："Type \""function handle\"" in Matlab’s \""Search documentation\"" bar and read more about this feature"
  [在Matlab的“搜索文档”栏中输入“function handle”，阅读更多关于此功能的内容]
- 英文文本："A function handle is a variable that can be used to call a function"
  [函数句柄是可用于调用函数的变量]
- 英文文本："z = 5.0"
  [z = 5.0]
- 英文文本："F = @(x,y) foo(x,z,y)"
  [F = @(x,y) foo(x,z,y)]
- 英文文本："What are the input variables that will be sent to F?"
  [将发送到F的输入变量是什么？]
- 英文文本："F is a handle to function foo"
  [F 是函数 foo 的句柄]
- 英文文本："F(2.0, 3.0) is the same as foo(2.0, 5.0, 3.0)"
  [F(2.0, 3.0) 与 foo(2.0, 5.0, 3.0) 等效]
- 英文文本："The CRCBPSO code accepts as input the handle to the fitness function to be optimized"
  [CRCBPSO 代码接受待优化适应度函数的句柄作为输入]
- 英文文本："This allows the same PSO code to be run on any fitness function"
  [这使得相同的粒子群优化代码可在任意适应度函数上运行]


### 第9页内容
- 英文文本："Tasks: Set 2"
  [任务：第二组]
- 英文文本："Write a script that does the following."
  [编写执行以下操作的脚本。]
- 英文文本："Assigns values to dataX and the parameter struct, P, sent to foo_new(dataX, SNR, P)"
  [为 dataX 和发送给 foo_new(dataX, SNR, P) 的参数结构体 P 赋值]
- 英文文本："For example:"
  [例如：]
- 英文文本：">> dataX = (0 : 499)/1024;"
  [>> dataX = (0 : 499)/1024;]
- 英文文本：">> P = struct(\'meaningful_name_of_param1\', 5.0, \'meaningful_name_of_param2\', 3.0, ...)"
  [>> P = struct('meaningful_name_of_param1', 5.0, 'meaningful_name_of_param2', 3.0, ...)]
- 英文文本："Of course, the specific values of dataX and those of the fields of P will be different for your case"
  [当然，dataX 的具体值以及 P 的字段值因你的场景而异]
- 英文文本："Creates a function handle to the function foo_new :"
  [为函数 foo_new 创建函数句柄：]
- 英文文本："H = @(x) foo_new(dataX, x, P);"
  [H = @(x) foo_new(dataX, x, P);]
- 英文文本："Uses the function handle to plot the time series of the signal for SNR values 10, 12, and 15"
  [使用函数句柄绘制 SNR 值为10、12和15时的信号时间序列]


### 第10页内容
- 英文文本："Anatomy of SDMBIGDAT19 codes"
  [SDMBIGDAT19 代码剖析]


### 第11页内容
- 英文文本："CRCBPSO: The PSO code"
  [CRCBPSO：粒子群优化代码]
- **Inputs（输入）板块**：
  - 英文文本："Handle to fitness function"
    [适应度函数句柄]
  - 英文文本："Fitness function initialized with parameters that will not change from one call to another in PSO"
    [适应度函数用在粒子群优化中多次调用时不变化的参数初始化]
  - 英文文本："Example: Data vector, PSD, sampling frequency"
    [示例：数据向量、功率谱密度（PSD）、采样频率]
  - 英文文本："Number of search space dimensions (= Number of parameters to maximize the likelihood Ratio over)"
    [搜索空间维度数（= 用于最大化似然比的参数数量）]
  - 英文文本："Example: 3 D search space for GLRT of quadratic chirp"
    [示例：二次 chirp 广义似然比检验的3维搜索空间]
- **Output（输出）板块**：
  - 英文文本："Structure containing:"
    [包含以下内容的结构体：]
  - 英文文本："Best fitness value found"
    [找到的最优适应度值]
  - 英文文本："Location of the best fitness"
    [最优适应度的位置]
  - 英文文本："Number of fitness evaluations (remember that particle fitness is not evaluated when they are outside the search space)"
    [适应度评估次数（注意：粒子在搜索空间外时，其适应度不评估）]
- **Notes（说明）板块**：
  - 英文文本："Standardized coordinates:"
    [标准化坐标：]
  - 英文文本："CRCBPSO searches within the unit \(D\)-dimensional hypercube: \(x_i \in [0,1],\ i = 1,2, ..., D\)"
    [CRCBPSO 在单位 \(D\) 维超立方体内搜索：\(x_i \in [0,1],\ i = 1,2, ..., D\)]
  - 英文文本："Location of the best fitness returned in standardized coordinates"
    [最优适应度的位置以标准化坐标返回]
  - 英文文本："Conversion to and from std. coord. is handled by the fitness function"
    [与标准化坐标的转换由适应度函数处理]
  - 英文文本："Each call to CRCBPSO is one run of PSO: Best - of - M - runs needs separate calls"
    [每次调用 CRCBPSO 是一次粒子群优化运行：多轮最优（Best - of - M - runs）需单独调用]


### 第12页内容
- 英文文本："CRCBPSO: The PSO code"
  [CRCBPSO：粒子群优化代码]
- 英文文本："% S = CRCBPSO(Fhandle, N)"
  [% S = CRCBPSO(Fhandle, N)]
- 英文文本："% Runs local best PSO on the fitness function with handle Fhandle. If Fname is the name of the function, Fhandle = @(x) <Fname>(x, FP), where FP is the set of parameters needed by Fname."
  [% 在适应度函数（句柄为 Fhandle）上运行局部最优粒子群优化。若 Fname 是函数名，则 Fhandle = @(x) <Fname>(x, FP)，其中 FP 是 Fname 所需的参数集合。]
- 英文文本："Example: FP would be a structure that includes the data vector, PSD etc."
  [示例：FP 可以是包含数据向量、功率谱密度等的结构体。]
- 英文文本："% N

【批次2处理结果：第13-20页图片内容】

### 第13页内容
- 英文文本："Simple fitness function"
  [简单适应度函数]
- 英文文本："Fitness functions should be able to handle multiple input locations"
  [适应度函数应能处理多个输入位置]
- 英文文本："F = CRCBPSOTESTFUNC(X,P)"
  [F = CRCBPSOTESTFUNC(X,P)]
- 英文文本："Compute the Rastrigin fitness function for each row of X. X is standardized, that is $0\leq X(i,j)\leq1$."
  [针对X的每一行计算Rastrigin适应度函数。X是标准化的，即$0\leq X(i,j)\leq1$。]
- 英文文本："Example of the matrix X for a 2D space"
  [二维空间下矩阵X的示例]
- 表格内容（参数示例）：
  - 第一行表头："Parameter 1" "Parameter 2"
  - 第一行数据："Location 1" "0.1" "0.8"
  - 第二行数据："Location 2" "0.5" "0.2"
- 英文文本："The fitness values are returned in F."
  [适应度值在F中返回。]
- 英文文本："Example of F for the above X"
  [上述X对应的F的示例]
- 表格内容（适应度示例）：
  - 第一行表头："Fitness"
  - 第一行数据："Location 1" "10.0"
  - 第二行数据："Location 2" "2.0"


### 第14页内容
- 英文文本："Simple fitness function"
  [简单适应度函数]
- 代码相关文本：
  - "F = CRCBPSOTESTFUNC(X,P)"
    [F = CRCBPSOTESTFUNC(X,P)]
  - "P has two arrays P.rmin and P.rmax that are used to convert X(i,j) internally to actual coordinate values before computing fitness:"
    [P包含两个数组P.rmin和P.rmax，用于在计算适应度前将X(i,j)内部转换为实际坐标值：]
  - "$X(:,j) \to X(:,j)*(rmax(j)-rmin(j))+rmin(j)$"
    [$X(:,j) \to X(:,j)*(rmax(j)-rmin(j))+rmin(j)$]
  - "Example for 2D space: P.rmin = $[-5,5]$; P.rmax = $[5,10] \Rightarrow$ range for parameter 1 is $[-5,5]$ and for parameter 2 is $[5,10]$"
    [二维空间示例：P.rmin = $[-5,5]$；P.rmax = $[5,10] \Rightarrow$ 参数1的范围是$[-5,5]$，参数2的范围是$[5,10]$]
  - "• The fields rmin and rmax are required for any fitness function"
    [• 任意适应度函数都需要rmin和rmax字段]
  - "• In your fitness function, the structure P will need more fields. Example: data, PSD, etc."
    [• 在你的适应度函数中，结构体P需要更多字段。例如：数据、PSD等。]
- 代码块（坐标转换与有效性检查）：
  - "%Check for out of bound coordinates and flag them"
    [%检查越界坐标并标记]
  - "validPts = crcbchkstdsrchrng(xVec);"
    [validPts = crcbchkstdsrchrng(xVec);]
  - "%Set fitness for invalid points to infty"
    [%将无效点的适应度设为无穷大]
  - "fitVal(~validPts)=inf;"
    [fitVal(~validPts)=inf;]
  - "%Convert valid points to actual locations"
    [%将有效点转换为实际位置]
  - "xVec(validPts,:) = s2rv(xVec(validPts,:),params);"
    [xVec(validPts,:) = s2rv(xVec(validPts,:),params);]


### 第15页内容
- 英文文本："Simple fitness function"
  [简单适应度函数]
- 代码相关文本：
  - "F = CRCBPSOTESTFUNC(X,P)"
    [F = CRCBPSOTESTFUNC(X,P)]
  - "The main part of this function"
    [函数的主要部分]
  - 代码块：
    ```matlab
    for lpc = 1:nrows
        if validPts(lpc)
            % Only the body of this block should be replaced for different fitness functions
            x = xVec(lpc,:);
            fitVal(lpc) = sum(x.^2-10*cos(2*pi*x)+10);
        end
    end
    ```


### 第16页内容
- 英文文本："Exercise #2"
  [练习#2]
- 英文文本："Run test_crcbpso.m: it runs crcbpso on the standard benchmark fitness function crcbpsotestfunc.m"
  [运行test_crcbpso.m：它在标准基准适应度函数crcbpsotestfunc.m上运行crcbpso]
- 英文文本："Experiment:"
  [实验：]
  - 英文文本："Change number of dimensions"
    [改变维度数]
  - 英文文本："Change search range"
    [改变搜索范围]
- 英文文本："Optional challenge: Learn how to use the surf function in Matlab and make the plot shown (see Matlab documentation for surf and worked out examples)"
  [可选挑战：学习如何在Matlab中使用surf函数并生成所示图表（查看Matlab中surf的文档及示例）]
- 左侧图片标注：[此处为原图中的图片：Rastrigin函数3D曲面图（x₁、x₂为坐标轴，fit(x₁,x₂)为高度）]


### 第17页内容
- 英文文本："Task: set 1"
  [任务：组1]
- 英文文本："Code a matlab function for any one benchmark fitness function following the example of crcbpsotestfunc.m"
  [遵循crcbpsotestfunc.m的示例，为任意一个基准适应度函数编写Matlab函数]
  - 英文文本："Ignore the fitness functions that have red highlights (there are typographical errors in them)"
    [忽略带红色高光的适应度函数（其中存在排版错误）]
  - 英文文本："Column \""D\"": Dimensionality of search space"
    [列“D”：搜索空间的维度]
  - 英文文本："Column \""Feasible Bounds\"": Range of each coordinate defining the search space, which is a hypercube"
    [列“Feasible Bounds”：定义搜索空间（超立方体）的每个坐标的范围]
- 英文文本："Apply crcbpso to the fitness function and find the solution for the global minimum and minimizer"
  [将crcbpso应用于适应度函数，找到全局最小值和极小值点的解]
- 英文文本："Experiment with different number of iterations (see additional features) and different number of runs in a best - of - M - runs strategy"
  [用不同的迭代次数（见附加功能）和不同的运行次数在best - of - M - runs策略下进行实验]
- 右侧表格标注：[此处为原图中的图片：基准函数表格（含方程、名称、维度D、可行边界等列）]
- 底部文本："Bratton, Kennedy, 2007: the paper has been provided to you"
  [Bratton, Kennedy, 2007：论文已提供给你]


### 第18页内容
- 英文文本："Best - of - M - runs: Parallelization"
  [Best - of - M - runs：并行化]
- 英文文本："If your Matlab installation has the Parallel computing toolbox (PCT), you can experiment with parallel computing"
  [如果你的Matlab安装了并行计算工具箱（PCT），你可以进行并行计算实验]
  - 英文文本："To check if you have PCT, run the command ver in Matlab"
    [要检查是否有PCT，在Matlab中运行ver命令]
- 英文文本："The easiest parallelization is for tasks that run completely independently of each other: this is the case for best - of - M - runs"
  [最简易的并行化适用于完全独立运行的任务：best - of - M - runs就是这种情况]
- 英文文本："For such tasks, a for loop can be replaced by a parfor loop; no other change is needed!"
  [对于此类任务，for循环可替换为parfor循环；无需其他修改！]
  - 英文文本："Read the documentation for parfor in Matlab"
    [阅读Matlab中parfor的文档]
- 英文文本："The advantage of parallelization will only be apparent if your computer’s CPU has multiple processing cores: you can find this out by looking at your system information (e.g., use \""About my Mac\"" under the Apple icon on a Mac; Google the required steps for a Windows machine)"
  [只有当你的计算机CPU有多个处理核心时，并行化的优势才会明显：你可以通过查看系统信息了解（例如，在Mac上点击Apple图标下的“关于本机”；在Windows机器上搜索所需步骤）]
  - 英文文本："If you are using parfor for the first time, Matlab may ask you for some configuration information: set the default number of parallel workers to be the number of processing cores"
    [如果你是第一次使用parfor，Matlab可能会要求你提供一些配置信息：将默认并行工作进程数设置为处理核心数]
- 英文文本："You can see parfor in action in test_crcbpso_par.m which does the same optimization as test_crcbpso.m but using 4 independent runs."
  [你可以在test_crcbpso_par.m中看到parfor的实际应用，它与test_crcbpso.m做相同的优化，但使用4次独立运行]


### 第19页内容
- 英文文本："CRCBPSO: Additional features"
  [CRCBPSO：附加功能]
- 代码说明文本：
  - "%S = CRCBPSO(Fhandle, N, P)"
    [%S = CRCBPSO(Fhandle, N, P)]
  - "%overrides the default PSO parameters with those provided in structure P."
    [%用结构体P中提供的参数覆盖默认的PSO参数。]
  - "%Set any of the fields of P to [] in order to invoke the corresponding default value. The fields of P are as follows."
    [%将P的任意字段设为[]以调用对应的默认值。P的字段如下。]
  - "%     \'popSize\': Number of PSO particles"
    [%     'popSize'：PSO粒子数]
  - "%     \'maxSteps\': Number of iterations for termination ;"
    [%     'maxSteps'：终止迭代次数 ;]
  - "%     \'boundaryCond\': Set to \'\' for the \"let them fly\" boundary condition."
    [%     'boundaryCond'：设为''表示“自由飞行”边界条件。]
  - "%                     Any other value is passed onto the fitness function for further processing. (NOT YET IMPLEMENTED PROPERLY)"
    [%                     任何其他值都会传递给适应度函数以作进一步处理。（尚未完全实现）]
  - "%     \'nbrhdSz\' : Number of particles in a ring topology neighborhood."
    [%     'nbrhdSz' ：环形拓扑邻域中的粒子数。]
  - "%                 Reset to 3 if less than 3."
    [%                 若小于3则重置为3。]
  - "%Setting P to [] will invoke the default values for all pso parameters."
    [%将P设为[]将调用所有PSO参数的默认值。]


### 第20页内容
- 英文文本："CRCBPSO: Additional features"
  [CRCBPSO：附加功能]
- 代码说明文本：
  - "%S = CRCBPSO(Fhandle, N, P, O)"
    [%S = CRCBPSO(Fhandle, N, P, O)]
  - "%O is an integer that controls the amount of information returned in S. The default value of O is zero and returns S as the struct defined above."
    [%O是控制S中返回信息量的整数。O的默认值为0，返回如上定义的结构体S。]
  - "%Progressively higher values of O increase the number of fields in S as listed below."
    [%O值逐渐增大时，S中的字段数如下增加。]
  - "%    \'allBestFit\': O = 1. Best fitness values for all iterations returned as a vector."
    [%    'allBestFit'：O = 1。所有迭代的最优适应度值作为向量返回。]
  - "%    \'allBestLoc\': O = 2. Best locations in standardized coordinates for all iterations returned as a row of a matrix."
    [%    'allBestLoc'：O = 2。所有迭代在标准化坐标中的最优位置作为矩阵的一行返回。]


【本批次处理完毕，可继续发送下一批次图片（格式：“批次3：第A - B页图片”），或告知“所有批次已提供完毕”以结束处理】

【批次3处理结果：第21-31页图片内容】

### 第21页内容
- 英文文本："GLRT with PSO"
  [结合粒子群优化的广义似然比检验]


### 第22页内容
- 英文文本："Plan"
  [计划]
- 英文文本："Using PSO to get GLRT for the Quadratic Chirp in Colored Gaussian Noise"
  [使用粒子群优化获取色高斯噪声中二次 Chirp 的广义似然比检验]
- 英文文本："1. Develop code for the fitness function (glrtqcsig.m)"
  [1. 编写适应度函数代码（glrtqcsig.m）]
- 英文文本："2. Develop code for calling PSO on the fitness function with multiple independent runs and picking the results from the best run"
  [2. 编写代码以多轮独立运行方式在适应度函数上调用粒子群优化，并选取最优轮次结果]
- 英文文本："3. Create a data realization and use codes to obtain GLRT using PSO"
  [3. 生成数据实现并使用代码通过粒子群优化获取广义似然比检验结果]
- 英文文本："4. Display results"
  [4. 展示结果]
- 流程模块文本（从左到右）：
  - "Modify code of glrtqcsig.m following crcbqcfitfunc"
    [遵循 crcbqcfitfunc 修改 glrtqcsig.m 代码]
  - "Modify code of function calling PSO: crcbqcpsc (new name)"
    [修改调用粒子群优化的函数代码：crcbqcpsc（新名称）]
  - "Supply data to crcbqcpsc and... magic!"
    [向 crcbqcpsc 提供数据并……见证效果！]


### 第23页内容
- 英文文本："Best-of-M runs"
  [多轮最优运行]
- 英文文本："Learn about Matlab’s parfor command"
  [了解Matlab的 parfor 命令]
- 代码示例（对应 crcbqcpsc.m 相关）：
```matlab
%Independent runs of PSO in parallel. Change 'parfor' to 'for' if the 
%parallel computing toolbox is not available.
parfor lpruns = 1:nRuns
    %Reset random number generator for each worker
    rng(lpruns);
    outStruct(lpruns)=crcbpso(fHandle,nDim,psoParams);
end
```
- 英文文本："Note how the pseudo-random number generator is reset on line 57: Each worker will get a separate seed but the seed will be the same in every call to crcbqcpsc"
  [注意第57行如何重置伪随机数生成器：每个工作进程会获取独立种子，但每次调用 crcbqcpsc 时种子相同]
- 英文文本："Controlling PRNG seeds ensures reproducibility when using stochastic optimization methods"
  [使用随机优化方法时，控制伪随机数生成器种子可确保可重复性]


### 第24页内容
- 英文文本："Fitness function"
  [适应度函数]


### 第25页内容
- 英文文本："Preliminaries"
  [预备知识]
- 英文文本："You have already coded the fitness function required for quadratic chirp in colored Gaussian noise"
  [你已编写好色高斯噪声中二次 Chirp 所需的适应度函数代码]
- 英文文本："You have to now cast it into a form that can be called by the PSO code"
  [你现在需将其转换为可被粒子群优化代码调用的形式]
- 英文文本："This is best done by taking the fitness function already coded for WGN and modifying it by replacing some parts"
  [最佳方式是选取已为白噪声（WGN）编写的适应度函数，通过替换部分内容来修改]


### 第26页内容
- 英文文本："Fitness function"
  [适应度函数]
- 代码相关文本：
  - "F = CRCBQCFITFUNC<glrtqcsig4pso>(X, P)"
    [F = CRCBQCFITFUNC<glrtqcsig4pso>(X, P)]
  - "Compute the fitness function (sum - of - squared residuals function after maximimzation over the amplitude parameter→log - likelihood ratio for colored noise maximized over the amplitude parameter) for data containing the quadratic chirp signal at the parameter values in X."
    [针对X中参数值对应的含二次 Chirp 信号的数据，计算适应度函数（对振幅参数最大化后的残差平方和函数→对色噪声在振幅参数上最大化的对数似然比）]
  - "The fitness values are returned in F."
    [适应度值在F中返回]
  - "X is standardized, that is \(0 \leq X(i,j) \leq 1\)."
    [X 是标准化的，即 \(0 \leq X(i,j) \leq 1\)]
  - "The fields P.rmin and P.rmax are used to convert X(i,j) internally before computing the fitness: \(X(:,j) \to X(:,j)*(rmax(j)-rmin(j)) + rmin(j). \Rightarrow \theta_i = x_i*(b_i - a_i)+a_i\)"
    [在计算适应度前，P.rmin 和 P.rmax 字段用于内部转换 X(i,j)：\(X(:,j) \to X(:,j)*(rmax(j)-rmin(j)) + rmin(j). \Rightarrow \theta_i = x_i*(b_i - a_i)+a_i\)]


### 第27页内容
- 英文文本："Fitness function"
  [适应度函数]
- 代码相关文本：
  - "F = CRCBQCFITFUNC<glrtqcsig4pso>(X, P)"
    [F = CRCBQCFITFUNC<glrtqcsig4pso>(X, P)]
  - "The fields P.dataY and P.dataX are used to transport the data and its time stamps. The fields P.dataX Sq and P.dataXCb contain the timestamps squared and cubed respectively. You will need an extra field to supply the PSD for colored noise."
    [P.dataY 和 P.dataX 字段用于传递数据及其时间戳。P.dataX Sq 和 P.dataXCb 字段分别包含时间戳的平方和立方形式。你需要额外字段来提供色噪声的功率谱密度（PSD）]
  - "[F,R] = CRCBQCFITFUNC<glrtqcsig4pso>(X,P) returns the quadratic chirp coefficients corresponding to the rows of X in R. Converts standardized to real (unstandardized) coordinates, i.e., the parameters \(a_1, a_2,\) and \(a_3\)"
    [[F,R] = CRCBQCFITFUNC<glrtqcsig4pso>(X,P) 在R中返回与X的行对应的二次 Chirp 系数。将标准化坐标转换为实际（非标准化）坐标，即参数 \(a_1, a_2,\) 和 \(a_3\)]
  - "[F,R,S] = CRCBQCFITFUNC<glrtqcsig4pso>(X,P) Returns the quadratic chirp signals corresponding to the rows of X in S. Converts the real coordinates to QC signal time series"
    [[F,R,S] = CRCBQCFITFUNC<glrtqcsig4pso>(X,P) 在S中返回与X的行对应的二次 Chirp 信号。将实际坐标转换为二次 Chirp 信号时间序列]


### 第28页内容
- 代码块及说明文本（适应度函数核心逻辑）：
```matlab
% Only the body of this block should be replaced for different fitness functions
fitVal(lpc) = ssrqc(x, params);

function ssrVal = ssrqc(x,params)← could call the fitness function glrtqcsig BUT...
%Generate normalized quadratic chirp
More efficient if the signal is generated inside this function for speed (calling crcbgenqcsig.m would be slow because dataX.^2 and dataX.^3 will be recomputed in every call but they need to be computed only once)
phaseVec = x(1)*params.dataX + x(2)*params.dataX Sq + x(3)*params.dataXCb;
qc = sin(2*pi*phaseVec);
qc = qc/norm(qc);→ norm is the one defined for WGN → Replace by the norm for given PSD; you have it in glrtqcsig.m; see DETEST/normsig4psd

%Compute fitness
ssrVal = -(params.dataY*qc')^2; → -⟨y, q(θ)⟩² for WGN → Replace by the inner product for given PSD; you have it in glrtqcsig.m; see DETEST/innerprodp sd
Test the ssrqc function independently... you can move it to a separate file and write a test script. For the same inputs it should give the same output as glrtqcsig.m.
```


### 第29页内容
- 英文文本："Tasks"
  [任务]
- 英文文本："Write a test script, test_glrtqcsig4pso.m, to do the following steps on glrtqcsig4pso.m."
  [编写测试脚本 test_glrtqcsig4pso.m，在 glrtqcsig4pso.m 上执行以下步骤]
  - 英文文本："You already have all the necessary codes from earlier exercises where you wrote glrtqcsig.m and then used it on data realizations data<n>.txt (n = 1, 2, 3) or on noise - only data realizations generated for a given PSD"
    [你已从之前的练习中获取所有必要代码，其中你编写了 glrtqcsig.m 并将其用于数据实现文件 data<n>.txt（n = 1, 2, 3）或给定功率谱密度下生成的纯噪声数据实现]
- 步骤说明：
  1. 英文文本："Create a data realization with colored Gaussian noise and SNR = 10 quadratic chirp signal."
     [生成含色高斯噪声和SNR = 10二次 Chirp 信号的数据实现]
     - 英文文本："Let the values of the parameters of this signal be \(a_1^{(0)}, a_2^{(0)}, a_3^{(0)}\), the superscript denoting that these are true parameters"
       [令该信号的参数值为 \(a_1^{(0)}, a_2^{(0)}, a_3^{(0)}\)，上标表示这些是真实参数]
  2. 英文文本："Create an array of values, A, for the parameter \(a_1\) with minimum and maximum values \(a_1^{min}\) and \(a_1^{max}\), respectively, and step size \(\Delta a_1\)"
     [为参数 \(a_1\) 创建值数组 A，其最小值和最大值分别为 \(a_1^{min}\) 和 \(a_1^{max}\)，步长为 \(\Delta a_1\)]
     - 英文文本："Ensure that \(a_1^{(0)} \in [a_1^{min}, a_1^{max}]\)"
       [确保 \(a_1^{(0)} \in [a_1^{min}, a_1^{max}]\)]
  3. 英文文本："Similarly, set ranges \([a_2^{min}, a_2^{max}]\) and \([a_3^{min}, a_3^{max}]\), such that \(a_2^{(0)} \in [a_2^{min}, a_2^{max}]\), \(a_3^{(0)} \in [a_3^{min}, a_3^{max}]\) (But no need to create an array of values here)"
     [类似地，设置范围 \([a_2^{min}, a_2^{max}]\) 和 \([a_3^{min}, a_3^{max}]\)，使得 \(a_2^{(0)} \in [a_2^{min}, a_2^{max}]\)、\(a_3^{(0)} \in [a_3^{min}, a_3^{max}]\)（但此处无需创建值数组）]
  4. 英文文本："Using the ranges above, create a matrix, X, such that its row \(i\) contains \([x_1^i, x_2^i, x_3^i]\), where
     \[
     x_1^i=\frac{A(i)-a_1^{min}}{a_1^{max}-a_1^{min}};\ x_2^i=\frac{a_2^{(0)}-a_2^{min}}{a_2^{max}-a_2^{min}};\ x_3^i=\frac{a_3^{(0)}-a_3^{min}}{a_3^{max}-a_3^{min}}
     \]
     "
     [使用上述范围，创建矩阵 X，使其第 \(i\) 行包含 \([x_1^i, x_2^i, x_3^i]\)，其中
     \[
     x_1^i=\frac{A(i)-a_1^{min}}{a_1^{max}-a_1^{min}};\ x_2^i=\frac{a_2^{(0)}-a_2^{min}}{a_2^{max}-a_2^{min}};\ x_3^i=\frac{a_3^{(0)}-a_3^{min}}{a_3^{max}-a_3^{min}}
     \]
     ]
     - 英文文本："In other words, we have constructed standardized coordinate values where \(x_j^i \in [0,1]\)"
       [换言之，我们已构造出 \(x_j^i \in [0,1]\) 的标准化坐标值]
  5. 英文文本："Using the data realization created in step 1, compute the fitness values for X using glrtqcsig4pso.m"
     [使用步骤1中生成的数据实现，通过 glrtqcsig4pso.m 计算X的适应度值]
  6. 英文文本："Plot the fitness values against the values in A: you should see the global minimum value near \(a_1^{(0)}\)"
     [将适应度值与A中的值绘图：你应在 \(a_1^{(0)}\) 附近看到全局最小值]


### 第30页内容
- 英文文本："Calling PSO"
  [调用粒子群优化]


### 第31页内容
- 英文文本："CRCBQCPSO: Inputs"
  [CRCBQCPSO：输入]
- 代码说明文本：
  - "%O = CRCBQCPSO<New Name>(I,P,N)"
    [%O = CRCBQCPSO<新名称>(I,P,N)]
  - "%I is the input struct with the fields given below."
    [%I 是含以下字段的输入结构体]
  - "% 'dataY': The data vector (a uniformly sampled time series)."
    [% 'dataY'：数据向量（均匀采样的时间序列）]
  - "% 'dataX': The time stamps of the data samples."
    [% 'dataX'：数据采样的时间戳]
  - "% 'dataX Sq': dataX.^2"
    [% 'dataX Sq'：dataX.^2]
  - "% 'dataXCb': dataX.^3"
    [% 'dataXCb'：dataX.^3]
  - "New fields needed for colored Gaussian noise:"
    [色高斯噪声所需的新字段：]
  - "'psd' : PSD values at positive DFT frequencies"
    ['psd' : 正离散傅里叶变换（DFT）频率处的功率谱密度值]
  - "'sampFreq' : Sampling frequency"
    ['sampFreq' : 采样频率]
  - "% 'rmin', 'rmax': The minimum and maximum values of the three parameters"
    [% 'rmin', 'rmax'：三个参数的最小值和最大值]
  - "%             \(a_1, a_2, a_3\) in the candidate signal:"
    [%             候选信号中的 \(a_1, a_2, a_3\)：]
  - "%             \(a_1*\text{dataX}+a_2*\text{dataX Sq}+a_3*\text{dataXCb}\)"
    [%             \(a_1*\text{dataX}+a_2*\text{dataX Sq}+a_3*\text{dataXCb}\)]
- 下方文本框（PSO参数结构体说明）：
  - "P is the PSO parameter %struct. Setting P to [] will invoke default parameters (see CRCBPSO)."
    [P 是粒子群优化参数结构体。将P设为[]将调用默认参数（见CRCBPSO）]
  - "% N is the number of independent PSO runs."
    [% N 是独立粒子群优化运行次数]
  - "%The output is returned in the struct O."
    [%输出在结构体 O 中返回]
 

【本批次处理完毕，可继续发送下一批次图片（格式：“批次4：第A - B页图片”），或告知“所有批次已提供完毕”以结束处理】

【批次3处理结果：第32-42页图片内容】

### 第32页：CRCBQCPSO: Outputs
- **标题**：CRCBQCPSO: Outputs [CRCBQCPSO：输出]
- **正文文本**：
  %The fields of O are: [O的字段如下：]
  % 'allRunsOutput': An N element struct array containing results from each PSO run. The fields of this struct are: ["allRunsOutput"：一个包含每次PSO运行结果的N元素结构体数组。该结构体的字段如下：]
  %     'fitVal': The fitness value. ["fitVal"：适应度值。]
  %     'qcCoefs': The coefficients [a1, a2, a3]. ["qcCoefs"：系数[a1, a2, a3]。]
  %     'estSig': The estimated signal. ["estSig"：估计的信号。]
  %     'totalFuncEvals': The total number of fitness evaluations. ["totalFuncEvals"：适应度评估的总次数。]
  % 'bestRun': The best run. ["bestRun"：最优运行结果。]
  % 'bestFitness': best fitness from the best run. ["bestFitness"：最优运行中的最优适应度。]
  % 'bestSig' : The signal estimated in the best run. ["bestSig"：在最优运行中估计的信号。]
  % 'bestQcCoefs' : [a1, a2, a3] found in the best run. ["bestQcCoefs"：在最优运行中得到的[a1, a2, a3]。]

### 第33页：Signal from estimated parameters
- **标题**：Signal from estimated parameters [基于估计参数的信号]
- **代码及注释**：
  %Prepare output [准备输出]
  fitVal = zeros(1,nRuns); [fitVal = zeros(1,nRuns);]
  for lpruns = 1:nRuns [for lpruns = 1:nRuns]
      fitVal(lpruns) = outStruct(lpruns).bestFitness; [fitVal(lpruns) = outStruct(lpruns).bestFitness;]
      outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns); [outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns);]
      [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation); [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation);]
      outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs; [outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs;]
      estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs); [estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs);]
      estAmp = inParams.dataY*estSig(:); [estAmp = inParams.dataY*estSig(:);]
      estSig = estAmp*estSig; [estSig = estAmp*estSig;]
      outResults.allRunsOutput(lpruns).estSig = estSig; [outResults.allRunsOutput(lpruns).estSig = estSig;]
      outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals; [outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals;]
  end [end]
- **标注框**：
  - “For each PSO run” [针对每次PSO运行]
  - “Convert standardized coordinates to QC parameters a1, a2, a3” [将标准化坐标转换为QC参数a1, a2, a3]

### 第34页：Signal from estimated parameters
- **标题**：Signal from estimated parameters [基于估计参数的信号]
- **代码及注释**：（与第33页代码一致，仅标注框不同）
  %Prepare output [准备输出]
  fitVal = zeros(1,nRuns); [fitVal = zeros(1,nRuns);]
  for lpruns = 1:nRuns [for lpruns = 1:nRuns]
      fitVal(lpruns) = outStruct(lpruns).bestFitness; [fitVal(lpruns) = outStruct(lpruns).bestFitness;]
      outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns); [outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns);]
      [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation); [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation);]
      outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs; [outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs;]
      estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs); [estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs);]
      estAmp = inParams.dataY*estSig(:); [estAmp = inParams.dataY*estSig(:);]
      estSig = estAmp*estSig; [estSig = estAmp*estSig;]
      outResults.allRunsOutput(lpruns).estSig = estSig; [outResults.allRunsOutput(lpruns).estSig = estSig;]
      outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals; [outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals;]
  end [end]
- **标注框**：“Store the QC parameters” [存储QC参数]

### 第35页：Signal from estimated parameters
- **标题**：Signal from estimated parameters [基于估计参数的信号]
- **代码及注释**：（与第33页代码一致，仅标注框不同）
  %Prepare output [准备输出]
  fitVal = zeros(1,nRuns); [fitVal = zeros(1,nRuns);]
  for lpruns = 1:nRuns [for lpruns = 1:nRuns]
      fitVal(lpruns) = outStruct(lpruns).bestFitness; [fitVal(lpruns) = outStruct(lpruns).bestFitness;]
      outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns); [outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns);]
      [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation); [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation);]
      outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs; [outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs;]
      estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs); [estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs);]
      estAmp = inParams.dataY*estSig(:); [estAmp = inParams.dataY*estSig(:);]
      estSig = estAmp*estSig; [estSig = estAmp*estSig;]
      outResults.allRunsOutput(lpruns).estSig = estSig; [outResults.allRunsOutput(lpruns).estSig = estSig;]
      outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals; [outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals;]
  end [end]
- **标注框**：“Use crcbgenqcsig to obtain the QC signal (signal generation within glrtqcsig4pso must be identical to crcbgenqcsig except for normalization)” [使用crcbgenqcsig获取QC信号（glrtqcsig4pso内的信号生成需与crcbgenqcsig一致，除了归一化）]

### 第36页：Signal from estimated parameters
- **标题**：Signal from estimated parameters [基于估计参数的信号]
- **代码及注释**：（与第33页代码一致，仅标注框不同）
  %Prepare output [准备输出]
  fitVal = zeros(1,nRuns); [fitVal = zeros(1,nRuns);]
  for lpruns = 1:nRuns [for lpruns = 1:nRuns]
      fitVal(lpruns) = outStruct(lpruns).bestFitness; [fitVal(lpruns) = outStruct(lpruns).bestFitness;]
      outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns); [outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns);]
      [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation); [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation);]
      outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs; [outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs;]
      estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs); [estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs);]
      estAmp = inParams.dataY*estSig(:); [estAmp = inParams.dataY*estSig(:);]
      estSig = estAmp*estSig; [estSig = estAmp*estSig;]
      outResults.allRunsOutput(lpruns).estSig = estSig; [outResults.allRunsOutput(lpruns).estSig = estSig;]
      outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals; [outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals;]
  end [end]
- **标注框**：“Caution: Normalization in crcbgenqcsig is for the case of WGN! Won’t work for colored noise” [注意：crcbgenqcsig中的归一化适用于白噪声（WGN）！不适用于有色噪声]

### 第37页：Signal from estimated parameters
- **标题**：Signal from estimated parameters [基于估计参数的信号]
- **代码及注释**：（与第33页代码一致，仅标注框和底部文本不同）
  %Prepare output [准备输出]
  fitVal = zeros(1,nRuns); [fitVal = zeros(1,nRuns);]
  for lpruns = 1:nRuns [for lpruns = 1:nRuns]
      fitVal(lpruns) = outStruct(lpruns).bestFitness; [fitVal(lpruns) = outStruct(lpruns).bestFitness;]
      outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns); [outResults.allRunsOutput(lpruns).fitVal = fitVal(lpruns);]
      [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation); [~,qcCoefs] = fHandle(outStruct(lpruns).bestLocation);]
      outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs; [outResults.allRunsOutput(lpruns).qcCoefs = qcCoefs;]
      estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs); [estSig = crcbgenqcsig(inParams.dataX,1,qcCoefs);]
      estAmp = inParams.dataY*estSig(:); [estAmp = inParams.dataY*estSig(:);]
      estSig = estAmp*estSig; [estSig = estAmp*estSig;]
      outResults.allRunsOutput(lpruns).estSig = estSig; [outResults.allRunsOutput(lpruns).estSig = estSig;]
      outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals; [outResults.allRunsOutput(lpruns).totalFuncEvals = outStruct(lpruns).totalFuncEvals;]
  end [end]
- **标注框**：“Replace!” [替换！]
- **底部文本**：
  1. Obtain estSig as shown,… [1. 如所示获取estSig，…]
  2. Normalize estSig again to have norm 1 according to the inner product $\langle \bar{x}, \bar{z} \rangle$ for colored noise [2. 针对有色噪声，根据内积$\langle \bar{x}, \bar{z} \rangle$再次归一化estSig使其范数为1]
  3. Obtain estimated amplitude: $A = \langle \bar{y}, \bar{q}(\Theta) \rangle$ (See derivation of GLRT in lecture) [3. 获取估计的振幅：$A = \langle \bar{y}, \bar{q}(\Theta) \rangle$（见课堂中GLRT的推导）]

### 第38页：Data realization
- **标题**：Data realization [数据实现]
[此处为原图中的图片：仅显示“Data realization”标题的页面]

### 第39页：Data realization
- **标题**：Data realization [数据实现]
- **列表内容**：
  - Number of samples: 512 [样本数量：512]
  - Sampling frequency: 512 Hz [采样频率：512 Hz]
  - Noise realization: Toy PSD (Note change of numbers) [噪声实现：玩具PSD（注意数值变化）]
    noisePSD = @(f) (f>=50 & f<=100).*(f-50).*(100-f)/625 + 1; [noisePSD = @(f) (f>=50 & f<=100).*(f-50).*(100-f)/625 + 1;]
    - You will need to generate the PSD at positive DFT frequencies [你需要在正DFT频率处生成PSD]
  - Signal: Quadratic chirp with the following parameters [信号：具有以下参数的二次 chirp]
    - SNR=10 [信噪比=10]
    - $a_1 = 10, a_2 = 3, a_3 = 3$ [$a_1 = 10, a_2 = 3, a_3 = 3$]
  - Data: Noise realization + signal [数据：噪声实现 + 信号]

### 第40页：GLRT calculation
- **标题**：GLRT calculation [广义似然比检验（GLRT）计算]
[此处为原图中的图片：仅显示“GLRT calculation”标题的页面]

### 第41页：PSO search range
- **标题**：PSO search range [粒子群优化（PSO）搜索范围]
- **列表内容**：
  - Number of independent PSO runs = 8 [独立PSO运行次数 = 8]
  - Number of iterations = 1000 [迭代次数 = 1000]
  - Search ranges [搜索范围]
    - $a_1 \in [1, 180]$ [$a_1 \in [1, 180]$]
    - $a_2 \in [1, 10]$ [$a_2 \in [1, 10]$]
    - $a_3 \in [1, 10]$ [$a_3 \in [1, 10]$]
  - $\Rightarrow$ We are searching over signals whose [$\Rightarrow$ 我们正在搜索满足以下条件的信号]
    - final instantaneous frequency is in the range $[1 + 2 + 3, 180 + 20 + 30] = [6, 230]$ Hz [最终瞬时频率在范围$[1 + 2 + 3, 180 + 20 + 30] = [6, 230]$ Hz内]
    - Initial instantaneous frequency is 1 Hz. [初始瞬时频率为1 Hz。]
  - This range is 89.4% of the widest possible range $[0, 256]$ Hz [该范围是最宽可能范围$[0, 256]$ Hz的89.4%]

【批次3补充处理结果：第42页完整内容】

### 第42页：Tasks
- **标题**：Tasks [任务]
- **列表内容**：
  - Write a Matlab script to do the following: [编写一个Matlab脚本以完成以下操作：]
    1. Generates the data realization as described in the “Data realization” slide [生成 “数据实现” 页面中描述的数据实现]
    2. Runs your modified version of crcbqcpsO using the settings given in the “PSO search range” slide [使用 “PSO搜索范围” 页面中给出的设置运行你修改后的crcbqcpsO版本]
    3. Makes a figure as follows: [生成如下图形：]
      - Plots the data realization [绘制数据实现]
      - Plots the true signal on top [在上方绘制真实信号]
      - Plots the best estimated signal on top [在上方绘制最优估计信号]
  - (Optional) plot the best signals from each PSO run in a different color: This will show the spread in performance of PSO: see SDMBIGDAT19/test_crcbqcpsO.m for inspiration! [(可选) 用不同颜色绘制每次PSO运行的最优信号：这将展示PSO性能的差异范围；参考SDMBIGDAT19/test_crcbqcpsO.m获取灵感！]
  - If you have done everything correctly, your best estimated signal should be a close match to the true signal [如果你所有操作都正确，你得到的最优估计信号应与真实信号高度匹配] 
- **右侧图表**：[此处为原图中的图片：包含数据散点与信号波形的图表]


【本批次处理完毕，可继续发送下一批次图片（格式：“批次4：第A - B页”），或告知“所有批次已提供完毕”以结束处理】