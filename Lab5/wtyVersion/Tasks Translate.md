# Tasks

## code needed

在SDMBIGDAT19/CODES中我们需要以下代码：
 r2ss.m：辅助函数；无需查看内部
 r2sv.m：辅助函数；无需查看内部
 s2rs.m：辅助函数；无需查看内部
 s2rv.m：辅助函数；无需查看内部
 crcbchkstdsrchrng.m：辅助函数；无需查看内部
 crcbpso.m：可应用于任何适应度函数的主PSO代码
 crcbpsotestfunc.m：一个基准适应度函数；也是如何编写与crcbpso.m配合使用的适应度函数的示例
 crcbqcfitfunc.m：用于二次啁啾GLRT（在白高斯噪声中）的适应度函数
 crcbqcpso.m：将PSO应用于二次啁啾适应度函数
 test_crcbpso.m：crcbpso.m的测试函数
 test_crcbqcpso.m：crcbqcpso.m的测试函数

## Task Set 1

- **目标**：修改你为生成指定信号所编写的函数，使其使用一个结构体（`struct`）作为输入参数来指定信号参数。
例如，若你的函数名为`foo(dataX, SNR, list_of_parameters)`，其中`list_of_parameters`要么是一个向量（如`SIGNALS/crcbgenqcsig.m`中那样），要么是一组输入参数，那么将参数封装在一个结构体（`P`）中传递：
  - 将`foo(dataX, SNR, list_of_parameters)`修改为`foo_new(dataX, SNR, P)`，其中`P`是一个结构体。
  - 例如，若你的参数为$a_1, a_2, a_3$，则结构体应为：
    - $P = \text{struct}('meaningful\_name\_of\_param1', a_1,'meaningful\_name\_of\_param2', a_2,...)$，其中你应使用能让代码读者了解参数性质的有意义的字段名。
你可查看`SDMBIGDAT19/CODES/s2rv.m`以了解如何将结构体传递给函数以及在函数内部如何使用它。
比较`foo_new`与`foo`在相同输入值下的输出，确保二者相同。
创建一个测试脚本以运行`foo_new`，该脚本需绘制（a）时间轴以秒为单位的信号时间序列图，以及（b）正频率的信号周期图（频率轴以赫兹为单位）。

## Task Set 2

 编写一个脚本，执行以下操作。
 为dataX和传递给foo_new(dataX, SNR, P)的参数结构体P赋值
 例如：
 >> dataX = (0:499)/1024;
 >> P = struct('有意义的参数1名称', 5.0, '有意义的参数2名称', 3.0, …)
 当然，对于你的情况，dataX的值以及P的字段值会不同
 创建到函数foo_new的函数句柄：
 H = @(x) foo_new(dataX, x, P);
 使用该函数句柄为信噪比（SNR）值为10、12和15时绘制信号的时间序列图


CRCBPSO：粒子群优化代码
11
输入
•适应度函数的句柄
•用在粒子群优化中每次调用都不会改变的参数初始化的适应度函数
•示例：数据向量、功率谱密度、采样频率
•搜索空间维度数（=要最大化似然比的参数数量）
•示例：二次线性调频信号广义似然比检验的3维搜索空间
输出
•包含以下内容的结构体：
•找到的最佳适应度值
•最佳适应度的位置
•适应度评估次数
（记住当粒子在搜索空间外时，不会评估其适应度）
注意事项
•标准化坐标：
•CRCBPSO在单位D维超立方体内搜索：
𝑥ᵢ ∈ [0,1]，
𝑖 = 1,2,…,𝐷
•返回的最佳适应度的位置是标准化坐标下的
•标准化坐标与非标准化坐标之间的转换由适应度函数处理
•每次调用CRCBPSO是一次粒子群优化运行：M次运行中的最佳需要单独调用







编写一个测试脚本test_glrtqcsig4pso.m，对glrtqcsig4pso.m执行以下步骤。
 你已经从之前的练习中拥有所有必要的代码，在那些练习中你编写了glrtqcsig.m并将其用于数据实现data<n>.txt（n = 1, 2, 3）或为给定功率谱密度生成的仅噪声数据实现
1. 创建一个带有彩色高斯噪声且信噪比为10的二次chirp信号的数据实现。
 设该信号的参数值为𝑎ₜᵣᵤₑ₁、𝑎ₜᵣᵤₑ₂、𝑎ₜᵣᵤₑ₃，上标表示这些是真实参数
2. 创建参数𝑎₁的取值数组𝐴，其最小值和最大值分别为𝑎₁ₘᵢₙ和𝑎₁ₘₐₓ，步长为Δ𝑎₁
 确保𝑎₁ᵢₙₜᵢₐₗ ∈ [𝑎₁ₘᵢₙ, 𝑎₁ₘₐₓ]
3. 类似地，设置范围[𝑎₂ₘᵢₙ, 𝑎₂ₘₐₓ]和[𝑎₃ₘᵢₙ, 𝑎₃ₘₐₓ]，使得𝑎₂ᵢₙₜᵢₐₗ ∈ [𝑎₂ₘᵢₙ, 𝑎₂ₘₐₓ]，𝑎₃ᵢₙₜᵢₐₗ ∈ [𝑎₃ₘᵢₙ, 𝑎₃ₘₐₓ]（但此处不需要创建取值数组）
4. 使用上述范围创建矩阵𝑋，使得其第𝑖行包含[𝑥₁ᵢ, 𝑥₂ᵢ, 𝑥₃ᵢ]，其中
𝑥₁ᵢ = (𝐴(𝑖,1) - 𝑎₁ₘᵢₙ) / (𝑎₁ₘₐₓ - 𝑎₁ₘᵢₙ)；𝑥₂ᵢ = (𝑎₂ᵢₙₜᵢₐₗ - 𝑎₂ₘᵢₙ) / (𝑎₂ₘₐₓ - 𝑎₂ₘᵢₙ)；𝑥₃ᵢ = (𝑎₃ᵢₙₜᵢₐₗ - 𝑎₃ₘᵢₙ) / (𝑎₃ₘₐₓ - 𝑎₃ₘᵢₙ)
 换句话说，我们构造了标准化坐标值，其中𝑥ᵢ ∈ [0,1]
5. 使用步骤1创建的数据实现，使用glrtqcsig4pso.m计算𝑋的适应度值
6. 将适应度值与𝐴中的值绘图：你应该会看到全局最小值出现在𝑎ₜᵣᵤₑ₁附近